def palindrome_recursive(chaine):
    if len(chaine) <= 1:
        return True
    
    if chaine[0] != chaine[-1]:
        return False
    
    return palindrome_recursive(chaine[1:-1])


mote1 = "radar"
mote2 = "hello"

print(mote1, "-->", palindrome_recursive(mote1))
print(mote2, "-->", palindrome_recursive(mote2))
